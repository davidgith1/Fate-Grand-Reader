# This should take the sprite sheets and make them into a single sprite that can be called with different expressions. 

import atlas_api as api

# Actually first let me test if the banner import works

# banner_test = api.AtlasAPI.get_war_banner(api.AtlasAPI(), 101)
# print(banner_test)

# ok that doesn't work (returns None) gotta debug



        

